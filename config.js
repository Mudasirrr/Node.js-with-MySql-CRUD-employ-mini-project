var config = {
	database: {
		host:	  'localhost', 	// database host
		user: 	  'root', 		// your database username
		password: '', 		// default MySQL port
		db:'employ' 		// your database name
	}
}

module.exports = config
